interface:
  display_name: "PPT Image Share Builder"
  short_description: "Clarify needs, then build image2 PPT page images, PPTX wrappers, and scripts."
  default_prompt: "Use $ppt-image-share-builder. First ask me the missing requirements, then turn my course topic, source files, and reference PPT style into image2 page prompts, generated PPT page images, a PPTX wrapper, and a timed presentation script."
policy:
  allow_implicit_invocation: true
