interface:
  display_name: "PPT Image Share Builder"
  short_description: "Build image2-first PPT image decks, PPTX files, and scripts."
  default_prompt: "Use $ppt-image-share-builder to turn my course topic, source files, and reference PPT style into image2 slide prompts, generated slide images, a PPTX, and a timed presentation script."
policy:
  allow_implicit_invocation: true
