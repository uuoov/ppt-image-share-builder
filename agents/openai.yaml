interface:
  display_name: "PPT Image Share Builder"
  short_description: "Build sourced PPT image decks and scripts."
  default_prompt: "Use $ppt-image-share-builder to turn my course topic, source files, and reference PPT style into slide image prompts, generated images, and a timed presentation script."
policy:
  allow_implicit_invocation: true
