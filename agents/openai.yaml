interface:
  display_name: "PPT Image Share Builder"
  short_description: "Run bounded intake, then build image2 PPT page images, PPTX wrappers, and scripts."
  default_prompt: "Use $ppt-image-share-builder. First inspect my materials and ask only the missing requirements, then turn my course topic, source files, and reference PPT style into image2 page prompts, generated PPT page images, a PPTX wrapper, and a timed presentation script."
policy:
  allow_implicit_invocation: true
